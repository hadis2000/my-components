import type { Meta } from "@storybook/react";
import { PackmanAnim } from "../components/pakman-animate";

const meta: Meta<typeof PackmanAnim> = {
  component: PackmanAnim,
  title: "PackmanAnim",
};
export default meta;

export const Default = {
  args: {},
};
