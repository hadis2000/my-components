import type { Meta } from "@storybook/react";
import { BallAnimate } from "../components/ball-animate";

const meta: Meta<typeof BallAnimate> = {
  component: BallAnimate,
  title: "BallAnimate",
};
export default meta;

export const Default = {
  args: {},
};
